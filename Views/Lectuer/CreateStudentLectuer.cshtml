@model SchoolSystem.Models.LectuerStudentViewModel

@{
    ViewData["Title"] = "Add Student In Lectuer";
}
<br>
<br>
<br>
<h1>Add Student To Lectuer</h1>
<hr /> 
<div class="row">
    <div class="col-md-4">
        <form asp-action="CreateStudentLectuer">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="IdLectuer" class="control-label">Lectuer</label>
                <select asp-for="IdLectuer" class="form-control" asp-items="ViewBag.Lec" disabled></select>
                <input type="hidden" asp-for="IdLectuer" />
            </div>
            <div class="form-group">
                <label asp-for="IdStudent" class="control-label">Student name</label>
                <select asp-for="IdStudent" class ="form-control" asp-items="ViewBag.IdStudent"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary"  />
                @{
                    var item = ViewBag.IdLectuer;
                        <a class="btn btn-dark" asp-action="ManagerLectuer" asp-route-idLectuer="@item">@item Back to The List</a>
                    
                }
                
                
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
