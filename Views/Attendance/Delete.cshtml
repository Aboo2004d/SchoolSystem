@model SchoolSystem.Data.Attendance

@{
    ViewData["Title"] = "Delete";
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Attendance</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.AttendanceStatus)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.AttendanceStatus)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.DateAndTime)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.DateAndTime)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Excuse)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Excuse)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.IdLectuerNavigation)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.IdLectuerNavigation.Id)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.IdStudentNavigation)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.IdStudentNavigation.Id)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.IdTeacherNavigation)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.IdTeacherNavigation.Id)
        </dd>
    </dl>
    
    <form id="attendanceDeleteForm">
        @Html.AntiForgeryToken()
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>
<script>
    document.getElementById('attendanceDeleteForm').addEventListener('submit', async function (event) {
        event.preventDefault();
        const response = await fetch('/api/Attendance/records/@Model.Id', {
            method: 'DELETE',
            credentials: 'same-origin',
            headers: { 'RequestVerificationToken': document.querySelector('input[name="__RequestVerificationToken"]').value }
        });
        const result = await response.json().catch(() => ({}));
        if (!response.ok) return alert(result.message || 'فشل حذف سجل الحضور.');
        window.location.href = result.redirectUrl;
    });
</script>
